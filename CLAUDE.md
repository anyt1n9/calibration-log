# 予測の答え合わせ（calibration-log）

自分の予測に確率を添えて記録し、期限が来たら ○ か ✕ を付ける。溜まった記録から、**確信度と実際の的中率のズレ**をキャリブレーション曲線で見せるアプリ。

React + Vite + TypeScript。`npm install` 後 `npm run dev`。

仕様は `docs/spec.md`。仕様から変えた点とその理由は `docs/changes.md`。**バランスや見せ方を触る前に両方読むこと。**

## 現在地

**フェーズ1は完成し、動作確認も済んでいる。** 型チェックと本番ビルドは通る。

指示がない限りフェーズ2（タグ絞り込み・通知・共有・AI補助・同期）には着手しないこと。まず全体の曲線が刺さるかを確かめる段階である。

## このアプリが測っているもの

的中率ではなく、**確信度の目盛りが合っているか**を測る。

- 「60%」と言って6割当たる人 → **満点**。4割外していても完璧
- 「95%」と言って7割しか当たらない人 → **失格**。よく当たっているのに失格

この区別がこのアプリの全てである。実装中に迷ったら、ここに戻ること。

## 絶対に守る設計原則

### 1. 誤差帯を必ず描く

キャリブレーション曲線の各点には、Wilson score interval による帯を重ねる。

n=3 で3回当たれば的中率は100%になる。点だけを描くと「90%帯は完璧だ」という**嘘の確信**が生まれる。帯があれば n=3 の点は 43.8%〜100% に広がり、「まだ何も言えない」が一目で伝わる。

**このアプリが数字で嘘をつかないための唯一の仕掛けである。** 見た目がうるさいと感じても外してはいけない。

### 2. 総合スコアを主役にしない

Brier score は画面の隅に小さく置くだけ。曲線が主役。

大きく出すと「スコアを上げるゲーム」になる。その最短手は**確信度50%で予測し続けること**であり、それでは何も学ばない。測るべきは精度ではなく歪みの形である。

大きく出す数字は**自信過剰度（平均確信度 − 的中率）ただ1つ**に限る。

### 3. 達成率を表示しない

これはタスク管理アプリではない。集計軸は日付ではなく確信度であり、「今日は何件できたか」は出さない。

出した瞬間、人は達成率を上げようとして安全な予測ばかりするようになり、測定そのものが壊れる。**「40%と言って40%しか起きなかった」は満点である**という考え方を崩す表示を足さないこと。

### 4. 予測文と確信度を後から編集させない

作成後に変更できるのは期限だけ（延期可、回数は記録に残す）。

編集できると、外れそうになったとき人は文を弱める。**過去の自分を守れてしまうと、このアプリは何も測れなくなる。**

### 5. 確信度は 50〜99 のみ

「起きない」と思うことは、文のほうを否定形で書いてもらう（「今月中に終わらない」70%）。

50未満を許すと、曲線を描くときに内部で反転処理が必要になり、**画面上の70%と集計上の70%がズレる**。

### 6. 逃げ道は用意し、逃げた回数は見せる

判定は ○ / ✕ / 判定不能 の3択。判定不能は集計から除外するが、件数は画面に出し続ける。

逃げ道がないと、判定が曖昧な予測を人は削除する。削除されると記録ごと消えて何も残らない。

### 7. 変化を見せることが演出である

判定ボタンを押した瞬間に全部が一斉に更新されてはいけない。カードの色 → 退場 → バッジ → 曲線の強調、と順に時間をかけて反映する。

**どの数字が自分の一押しで動いたのか**が分かることが演出の目的。装飾やイラストで「アプリらしさ」を出そうとしないこと。

## 技術方針

- 状態管理は `useReducer`。外部の状態管理ライブラリは入れない
- **曲線は SVG を直接記述する。チャートライブラリは使わない**
- 調整用の定数は `src/config/constants.ts` に集約する。コンポーネント内に散らさないこと
- 統計計算は `src/lib/calibration.ts` に純関数として置く。UIから独立させ、Nodeで検証できる形を保つ
- 日付計算に外部ライブラリを使わない。`src/lib/date.ts` の範囲に留める
- 保存は localStorage のみ。サーバーもアカウントも作らない

## コードを書くときの注意

- 表示する数値は必ず `Math.round()` か `.toFixed()` を通す
- ダークモードに対応する。色は CSS 変数で定義し、ハードコードしない
- 開発用の機能（ダミーデータ生成・日付送り）は `import.meta.env.DEV` で囲い、本番ビルドに含めない

### 既に踏んだ罠

**「読み込み済みか」を `useRef` で持ってはいけない。**

React は宣言順にエフェクトを実行する。読み込みエフェクトの中で ref を立てると、同じコミットで走る保存エフェクトがそれを見てしまい、まだ初期値のままの**空配列が本物のデータを上書きする**。リロードで全データが消えた。

レンダリングに反映されるべき状態は reducer の state に置くこと（`loaded`）。詳細は `docs/changes.md` の6番。

## 保留中の判断（勝手に進めないこと）

以下はユーザーがまだ決めていない。提案済みだが選択は保留されている。

- GitHub Pages での公開（完全な静的サイトなので Render 等は不要）
- GitHub Actions での CI（型チェック・ビルド）
- リポジトリの About 欄と Topics
- Vitest による統計計算のテスト（現状テストコードは無く、手動検証のみ）

## 使ってみないと分からないこと

自信過剰度は判定が10件（`MIN_RESOLVED_FOR_HEADLINE`）溜まるまで表示されない。最初の予測から最短でも数週間かかる。

この「何も表示されない期間」に人が耐えられるかは、ダミーデータでは検証できない。実際に使ってみて退屈すぎるようなら、閾値を下げるか途中経過の見せ方を足す。
